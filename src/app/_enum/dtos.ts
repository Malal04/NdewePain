export enum Role {
    ADMIN = 'admin',
    GERANT = 'gerant',
    EMPLOYE = 'employe',
    CLIENT = 'client',
    USER = 'user',
    CUSTOMER = 'customer',
}

export enum AccountState {
    ACTIVED = 'actived',
    BLOCKED = 'blocked'
}
